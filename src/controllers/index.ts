import destination from "./destination";
import dispatch from "./dispatch";
import { zone } from "./zone.controller";

const controllers = {
  zone: zone,
  destination: destination,
  dispatch: dispatch,
};

export default controllers;
